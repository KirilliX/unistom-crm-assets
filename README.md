# Unistom CRM frontend assets

Public, compiled frontend assets for the Unistom CRM deployment.

The repository contains only browser-delivered JavaScript, CSS, fonts, icons,
and PWA files. Application data, credentials, server configuration, and source
maps are not included.

The assets are distributed through jsDelivr so the CRM interface remains
available independently of the origin server's static-file network path.
